import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: {
    flex: 1,
    width: '80%',
    height: '80%',
    justifyContent: 'space-around',
    alignItems: 'center',
    backgroundColor: '#7A6AC3',
  },
})

export default styles
